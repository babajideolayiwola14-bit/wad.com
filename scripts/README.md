# Maintenance scripts

One-off utilities for database inspection and migration. **Not used at runtime.**

Requires `DATABASE_URL` (PostgreSQL). Run from project root:

```bash
node scripts/check-db.js
```

Legacy scripts that only supported SQLite may need updates before use.
