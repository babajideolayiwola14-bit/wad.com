# Documentation archive

These files were written during an earlier security-hardening pass (including Heroku/SQLite references). The app now targets **PostgreSQL on Render**.

For current setup, see the root [README.md](../README.md).
